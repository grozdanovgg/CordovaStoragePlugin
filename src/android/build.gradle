repositories{    
  mavenCentral()
  flatDir {
      dirs 'libs'
   }
}

dependencies {
   compile 'com.google.code.gson:gson:2.8.0'
}

android {
  packagingOptions {
    exclude 'META-INF/NOTICE'
    exclude 'META-INF/LICENSE'
  }
}